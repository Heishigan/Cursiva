# cloudbuild.yaml — Google Cloud Build CI/CD pipeline for the Cursiva backend
#
# Triggers on every push to `main` that touches:
#   - backend/**
#   - cloudbuild.yaml
#
# Required Cloud Build service account permissions:
#   - roles/run.admin
#   - roles/iam.serviceAccountUser
#   - roles/artifactregistry.writer

substitutions:
  _REGION: europe-north1
  _SERVICE: cursiva-backend
  _REPO: cursiva-backend

timeout: 1800s  # 30-minute hard cap for the entire pipeline

steps:
  # ── Step 1: Pull the previous image to use as a layer cache ───────────────
  - id: pull-cache
    name: 'gcr.io/cloud-builders/docker'
    entrypoint: bash
    args:
      - '-c'
      - |
        docker pull ${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/${_SERVICE}:latest || echo "No cache image found, building from scratch."
    timeout: 120s

  # ── Step 2: Build the Docker image ────────────────────────────────────────
  - id: build
    name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '--cache-from'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/${_SERVICE}:latest'
      - '-t'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/${_SERVICE}:$COMMIT_SHA'
      - '-t'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/${_SERVICE}:latest'
      - '--label'
      - 'commit=$COMMIT_SHA'
      - '--label'
      - 'build=$BUILD_ID'
      - './backend'
    timeout: 1500s
    waitFor: ['pull-cache']

  # ── Step 3: Push both tags to Artifact Registry ───────────────────────────
  - id: push
    name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - '--all-tags'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/${_SERVICE}'
    timeout: 300s
    waitFor: ['build']

  # ── Step 4: Deploy to Cloud Run ───────────────────────────────────────────
  - id: deploy
    name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - '${_SERVICE}'
      - '--image'
      - '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/${_SERVICE}:$COMMIT_SHA'
      - '--region'
      - '${_REGION}'
      - '--platform'
      - 'managed'
      - '--quiet'
    timeout: 300s
    waitFor: ['push']

  # ── Step 5: Smoke-test — verify the service is healthy after deploy ────────
  - id: health-check
    name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: bash
    args:
      - '-c'
      - |
        SERVICE_URL=$$(gcloud run services describe ${_SERVICE} \
          --region ${_REGION} \
          --format 'value(status.url)')
        echo "Smoke-testing $$SERVICE_URL..."
        HTTP_CODE=$$(curl --silent --output /dev/null --write-out "%{http_code}" "$$SERVICE_URL/")
        if [ "$$HTTP_CODE" -ne 200 ]; then
          echo "Health check failed! HTTP $$HTTP_CODE"
          exit 1
        fi
        echo "Health check passed (HTTP $$HTTP_CODE)."
    timeout: 60s
    waitFor: ['deploy']

images:
  - '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/${_SERVICE}:$COMMIT_SHA'
  - '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/${_SERVICE}:latest'

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'
